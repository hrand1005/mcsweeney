package main

import (
// "testing"
)

/*
func TestClipService(t *testing.T) {
  // Create Clip Service hitting Mock API
  // Test that clips are properly filtered
  // Test improper credentials error case
  // Test no more clips found case
  // Test timeout request case
  tests := []struct{
    name string
    service ClipService
    want []string
  }{
    {
      name: "Nominal case returns clip",
      service: ClipService{
        filter: MockFilter,
        query: MockQuery,
        credentials: validCredentials,
      },
    },
  }
}
*/
